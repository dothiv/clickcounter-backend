cron:

- description: persist memcache click counts
  url: /task/counter/persist
  schedule: every 1 minutes

- description: persist total click counts
  url: /task/total/persist
  schedule: every 1 hour